# ========================================
# SERVER CONFIGURATION
# ========================================
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# ========================================
# DATABASE CONFIGURATION
# ========================================

# For LOCAL DEVELOPMENT (Direct Connection - Port 5432)
# Use this for localhost development with direct Supabase connection
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.vokbvbdzvturbvmcuocz.supabase.co:5432/postgres

# For VERCEL DEPLOYMENT (Connection Pooler - Port 6543)
# Get this from: Supabase Dashboard → Settings → Database → Connection Pooling
# IMPORTANT: Use Transaction mode (port 6543) for serverless environments like Vercel
# Set this as environment variable in Vercel Dashboard
DATABASE_POOLER_URL=postgresql://postgres.vokbvbdzvturbvmcuocz:YOUR_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# ========================================
# JWT CONFIGURATION
# ========================================
# Generate with: openssl rand -base64 32
# NEVER use the example secret in production!
JWT_SECRET=your_jwt_secret_key_here_generate_random_string_minimum_32_chars

# ========================================
# EMAIL CONFIGURATION (SMTP)
# ========================================
# For Gmail: Get App Password from https://myaccount.google.com/apppasswords
# Enable 2-Factor Authentication first, then generate App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your_gmail_app_password_here
FROM_NAME=Shortifi

# ========================================
# GOOGLE OAUTH CONFIGURATION
# ========================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Google OAuth Redirect URIs (configure in Google Cloud Console):
# - Local Development: http://localhost:3000/google/callback
# - Production: https://your-api-domain.vercel.app/google/callback

# ========================================
# VERCEL DEPLOYMENT NOTES
# ========================================
# 1. Set all environment variables in Vercel Dashboard → Settings → Environment Variables
# 2. Use DATABASE_POOLER_URL (not DATABASE_URL) for production
# 3. Set NODE_ENV=production for production deployment
# 4. Update FRONTEND_URL to your deployed frontend URL
# 5. Add production callback URL to Google OAuth settings

# ========================================
# SECURITY NOTES
# ========================================
# ⚠️ NEVER commit .env file to git (it's in .gitignore)
# ⚠️ Generate new JWT_SECRET for production (minimum 32 characters)
# ⚠️ Use different secrets for development and production
# ⚠️ Rotate secrets regularly for production apps
