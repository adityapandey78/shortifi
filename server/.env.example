# Server Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173



DATABASE_URL=

# DATABASE_POOLER_URL= ££for production databases with pooler support
# JWT Secret - Generate with: openssl rand -base64 32
JWT_SECRET=

# Gmail SMTP (for email verification) - Use App Password from https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
FROM_NAME=

# Google OAuth  - Get from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Legacy MySQL (deprecated - only for old migration scripts)
# DATABASE_HOST=localhost
# DATABASE_USER=root
# DATABASE_PASSWORD=your_mysql_password_here
# DATABASE_NAME=url_shortener
